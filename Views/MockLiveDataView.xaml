<Window x:Class="iRacingLiveDataOverlay.Views.MockLiveDataView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:iRacingLiveDataOverlay.Views"
        mc:Ignorable="d"
        Title="MockLiveDataView"
        WindowStyle="None"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        FontSize="18"
        Background="LightGray" d:DesignHeight="450" d:DesignWidth="800"
        Width="{Binding Source={x:Static SystemParameters.FullPrimaryScreenWidth}}"
        Height="{Binding Source={x:Static SystemParameters.FullPrimaryScreenHeight}}">
    <Window.Resources>
        <Style x:Key="RobotoL">
            <Setter Property="TextElement.FontFamily" Value="Utility/Fonts/Roboto/Roboto-Light"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DataGrid Foreground="White" Background="Transparent" x:Name="MockDrivers" AlternatingRowBackground="Transparent" 
                   Grid.Row="0" Grid.Column="0" Height="300" Width="600" VerticalScrollBarVisibility="Hidden" FontFamily="Arial"
                   AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="None" RowBackground="{StaticResource TransBlackPearl}" BorderThickness="0">
            <DataGrid.Columns>
                <DataGridTextColumn SortDirection="Descending" Header="Pos" Binding="{Binding Path=Position}" Width="60"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Path=Name}" Width="200"/>
                <DataGridTextColumn Header="IR" Binding="{Binding Path=IRating}" Width="50"/>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</Window>
