<Window x:Class="iRacingLiveDataOverlay.Views.LiveDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             WindowStyle="None"
             AllowsTransparency="True"
             WindowStartupLocation="CenterScreen"
             Topmost="False"
             FontSize="14"
             Background="Transparent"
             Width="{Binding Source={x:Static SystemParameters.FullPrimaryScreenWidth}}"
             Height="{Binding Source={x:Static SystemParameters.FullPrimaryScreenHeight}}"
             d:DesignHeight="450" d:DesignWidth="800" FontFamily="Segoe UI Light" Foreground="{StaticResource Clouds}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DataGrid  Background="{StaticResource TransparentAsphalt}" x:Name="CurrentDrivers" AlternatingRowBackground="Gray" 
                   Grid.Row="0" Grid.Column="0" Height="300" Width="800"
                   AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path=Position}" Width="30"/>
                <DataGridTextColumn Binding="{Binding Path=CarNumber}" Width="30"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Path=Name}" Width="200"/>
                <DataGridTextColumn Header="IR" Binding="{Binding Path=Rating}" Width="50"/>
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding Path=SessionInfo, Mode=OneWay}"></TextBlock>
        <TextBlock Grid.Row="2" Grid.Column="0" Text="{Binding Path=DriverInfo, Mode=OneWay}"></TextBlock>
    </Grid>
</Window>
